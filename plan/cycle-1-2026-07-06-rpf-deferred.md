# Cycle 1 (2026-07-06) RPF Deferred Findings

Register of review findings from `.context/reviews/_rpf-2026-07-06-cycle1-aggregate.md`
NOT implemented this cycle. Severity/confidence are recorded as reviewed (no downgrades).
Deferral policy sources consulted (in order): `CLAUDE.md`, `AGENTS.md`, `.context/**`,
`docs/`. Repo precedent for deferring architectural-scale security work with explicit exit
criteria: `plan/cycle-4-2026-07-03-deferred.md` (C4-US-006..023) and
`plan/cycle-2-2026-07-02-deferred.md`. Deferred work stays bound by repo policy when picked
up (GPG-signed conventional+gitmoji commits, no `--no-verify`, no force-push).

Items marked CROSS-REF are already tracked in a prior deferred plan with valid exit
criteria and are NOT re-opened here (per cycle instructions).

## HIGH

| ID | Finding (file:line) | Severity/Conf | Reason for deferral | Exit criterion |
|---|---|---|---|---|
| D-H5 | Docker socket proxy POST=1 permits arbitrary container-create HostConfig (Privileged, host binds) → worker RCE = host takeover. `docker-compose.production.yml:65-95`, `docker-compose.worker.yml:19-46` | HIGH/High | CROSS-REF `plan/cycle-4-2026-07-03-deferred.md` C4-US-010 (socket-proxy privilege reduction) — architectural change (gVisor rollout via `scripts/install-gvisor.sh` and/or authz-filtering proxy) requiring infra validation on worker hosts; aggregate itself classifies as deferred-class and directs cross-reference, not re-open. | C4-US-010 exit criteria: proxy restricted to read-only endpoints required by app/worker; minimal permission set documented; compose-render test rejects mutable socket-proxy permissions. Additional: gVisor or authz proxy enforcing image whitelist + rejecting Privileged/host binds on worker hosts. |
| D-SR-H1 | Judge API internet-reachable; integrated hosts historically defaulted `JUDGE_ALLOW_ANY_JUDGE_IP=1`; nginx `location /api/v1/judge/` has no allow/deny (`deploy-docker.sh:312-313,1698`) | HIGH/High | Partially mitigated at HEAD: commit 784840cf (2026-07-05) made JUDGE_ALLOW_ANY target-aware, and C4-US-007 landed the fail-closed app-level IP allowlist; remaining ask is a proxy-layer allowlist. Deriving correct worker source IPs per host (Docker NAT, integrated worker calling via public URL) requires per-host network verification; a wrong allow/deny takes judging down fleet-wide. Overlaps C4-053 (allowlist auto-population, deferred in cycle-4 plan). Auth today: 32-char shared register token → per-worker secret (verified working). | nginx judge location carries `allow <verified worker source IPs/subnets>; deny all;` (or internal-only listener) on both targets, validated by a live worker register/claim after deploy; JUDGE_ALLOWED_IPS set even on integrated hosts. |
| D-CQ-H1 | Backup "streamBackupWithFiles" buffers whole DB JSON + all uploads + full ZIP in memory → OOM on production-size DB. `src/lib/db/export-with-files.ts:248-326` | HIGH/High | Availability (OOM) — not data-loss; the sibling restore-side buffering is already deferred as C4-US-014 ("Partially mitigated ... ZIP restore path buffers the entire archive in memory"). Same streaming rework (JSZip → archiver/yazl or generateInternalStream) should land as one change with C4-US-014; too large to land safely alongside this cycle's CRITICAL data-loss fix in the same subsystem. | Extend C4-US-014: backup generation streams db.json and each upload into the archive without whole-archive buffering; memory-usage regression test/assertion for large export AND restore payloads. |
| D-CQ-H2 | Single global SSE advisory lock serializes all slot acquisition. `src/lib/realtime/realtime-coordination.ts:101` | HIGH(perf)/High | CROSS-REF C4-US-009 (real-time coordination lock bottleneck) — identical finding, already tracked with exit criteria. | C4-US-009 exit criteria (per-resource locks or Redis-backed coordination + load benchmark + concurrency regression test). |

## MEDIUM

| ID | Finding (file:line) | Severity/Conf | Reason for deferral | Exit criterion |
|---|---|---|---|---|
| D-M3 | Blocking docker-run cleanup inside async Drop; temp dir leaks if docker/alpine unavailable. `judge-worker-rs/src/workspace.rs:96-105` | MED/High | Resource-leak/latency, not judging correctness; async Drop redesign (spawn_blocking / deferred reaper task + periodic temp-dir sweep) needs load validation on worker hosts; Drop currently guarantees best-effort cleanup that a rushed async refactor could break (Drop cannot await). | Cleanup moved off the async runtime thread (spawn_blocking or queued reaper); periodic sweep reaps orphaned 65534-owned workspace dirs; leak regression test. |
| D-M4 | seccomp-profile.json allows `clone` with no CLONE_NEWUSER arg filter. `docker/seccomp-profile.json:28` | MED/Med | Sandbox-hardening (defense-in-depth; `unshare`/`setns` already denied, no-new-privileges set). An arg-filtered clone rule copied from Docker's default profile must be validated against all ~80 language images (glibc/musl thread creation) on worker-0 before production; a wrong mask breaks every threaded runtime. | Arg-filtered clone rule (Docker-default mask 0x7E020000 CLONE_NEW* bits) added and full language-image judge smoke suite passes on a staging worker; or documented decision that userns is disabled kernel-wide. |
| D-M8 | crun binary curl-installed with no checksum/signature, set as default Docker runtime. `scripts/install-crun-runtime.sh:62-69` | MED/High (supply-chain) | Script is operator-run provisioning tooling, not invoked by deploy path this cycle (deploy uses existing runtime; gvisor script already verifies). Pinning requires selecting a crun release + obtaining its published digest/signature out-of-band; blind TOFU checksum adds no integrity. | Script pins an exact crun version and verifies the upstream-published sha256/GPG signature before install (mirroring `scripts/install-gvisor.sh:44-50`); refuses to install on mismatch. |
| D-M14 | docker-socket-proxy `:latest` + IMAGES=1 in test-backends compose. `docker-compose.test-backends.yml:60,65,71` | MED/Med | Stack is the worv/test.worv.ai backend, removed from the active deploy roster this cycle (U1); file stays in-tree but is not deployed anywhere. Hardening a dead stack is not this cycle's scope. | Either the test-backends stack is deleted from the repo, or (if revived) its socket proxy is digest-pinned with IMAGES=0 like production/worker compose files. |
| D-SR-M4 | Local-compiler fallback can run untrusted code on app server when `COMPILER_RUNNER_URL` unset in production. `src/lib/compiler/execute.ts:104-105` (prod-gated only by seccomp presence :841) | MED/High | Making missing COMPILER_RUNNER_URL a fatal boot error can hard-down production if any current host legitimately boots without it (integrated host wiring must be audited on both targets first); mitigations at HEAD: seccomp-presence prod gate + admin-only local escape hatch. Aligns with prior-tracked local-fallback items (aggregate "Verified-still-open" list, cycle-2 deferred). | Deploy env audit confirms both targets always set COMPILER_RUNNER_URL; then NODE_ENV=production boot fails fast when unset (with explicit override env for dev-like deployments); boot test added. |
| D-SR-M6b | Anti-cheat heartbeat: per-event JWT uaHash verification + uaMismatch flagging (second half of SR-M6). `src/app/api/v1/contests/[assignmentId]/anti-cheat/route.ts` | MED/Med | Origin-check NODE_ENV gate removal (first half) lands this cycle (C6). uaHash-per-event needs token payload changes + client replay analysis; exam-integrity signal quality, not an access-control breach (endpoint already requires authenticated session + contest token). | Heartbeat verifies JWT uaHash per event, flags uaMismatch into antiCheatEvents, with tests. |
| D-CQ-M3 | Export: 50ms-poll backpressure + O(n²) OFFSET pagination inside long REPEATABLE READ tx pins snapshot/connection. `src/lib/db/export.ts:88-181` | MED/Med | Perf/operational; correct behavior preserved. Spool-to-tempfile + keyset pagination is the same subsystem rework as D-CQ-H1/C4-US-014 — do together. | Export spools to temp file (or keyset-paginates) so no client-paced long-lived tx; export deadline enforced; covered by the C4-US-014 streaming rework. |
| D-CQ-M4 | Restore zip limits trust attacker-declared sizes; decompressed bytes unbounded; manifest optional for legacy zips (zip-bomb DoS). `src/lib/db/export-with-files.ts:133-159,213,368-370` | MED/Likely | Restore endpoint is admin-only (capability-gated) so the DoS requires an admin principal; the fix (hard byte-count Transform cap + mandatory manifest) belongs in the same export-with-files streaming rework (D-CQ-H1/C4-US-014) rather than a second conflicting patch to the file in one cycle. | Hard actual-byte cap enforced in the staging Transform (abort past limit); manifest required for restore; zip-bomb regression test. |
| D-CQ-M5 | SIGTERM/SIGINT handlers neither exit nor re-raise; may stall `docker stop`. `src/lib/audit/node-shutdown.ts:29-51` | MED/Low | Shutdown-path change interacts with Next.js/standalone server signal handling; wrong ordering can cut off in-flight audit flush it exists to protect. Needs a container-level stop test, not unit-only. | Handler awaits flush then re-raises default signal behavior (or process.exit) verified by a docker-stop integration test showing termination <10s with flush completed. |
| D-N1..N14 | Architect structural findings: assignments table overload (N1), ON DELETE asymmetry destroying exam-integrity evidence (N2, HIGH-integrity), scoring logic triplication (N4), 18 routes bypassing createApiHandler (N7), PostgreSQL SPOF (N14), plus N5/N6/N8/N9/N10/N11/N12/N13 hygiene (schema.pg.ts:329-379,479-481,...) | HIGH..LOW/Med | Architectural-scale (schema migrations, route-framework migration, HA topology). Several CROSS-REF cycle-4 deferred items (C4-039 config sprawl, C4-089 enum CHECKs ≈ N-item overlap). N2 involves ON DELETE semantics on live data — requires a migration plan + backup rehearsal, not a cycle patch. N12 (dead drizzle dialects), N13 (.context tracked-but-gitignored), dual plan-dir note: this cycle keeps `plan/` as the single active dir. | Each N-item gets a dedicated design doc + migration plan before implementation; N2 exit: examSessions/codeSnapshots/antiCheatEvents survive assignment deletion (SET NULL + retention) or documented retention decision; N7 exit: route audit shows all API routes emit shared security headers/requestId. |
| D-PERF | Perf re-confirms: no statement_timeout; no compose resource limits; unbounded req.json(); compiler p-limit queue 128MiB accumulation; full-buffer uploads; global SSE lock | MED/Med | CROSS-REF: global SSE lock = C4-US-009; upload buffering = C4-US-014; remaining items are capacity-tuning needing load tests (perf reviewer's own file was not finished — re-review next cycle). | statement_timeout + compose mem/cpu limits set and load-tested; body-size caps on shared handler; compiler queue bounded with backpressure. |

## LOW

| ID | Finding (file:line) | Severity/Conf | Reason for deferral | Exit criterion |
|---|---|---|---|---|
| D-L1 | rate-limiter `/check` + `/record-failure` double-count attempts (fails stricter/safe). `rate-limiter-rs/src/main.rs:263,318` | LOW/Med | Fails-safe (over-limits, never under-limits); semantic fix belongs to C4-US-012 single-source-of-truth rework (CROSS-REF cycle-4 deferred). | C4-US-012 exit criteria (PG authoritative counters; consistency tests). |
| D-L4 | Exec-able 1GiB run-phase /tmp; no disk quota on compile workspace. `judge-worker-rs/src/docker.rs:361-366,328-332` | LOW/Med | Sandbox capacity tuning; shrinking /tmp or noexec risks breaking runtimes that JIT/写 temp artifacts — needs per-language validation on worker-0. | /tmp mounted noexec (or minimized) + compile workspace quota with full language smoke suite green. |
| D-L5 | `deploy-docker.sh` SSH_OPTS/SSH_KEY expanded unquoted (breaks on paths with spaces). | LOW/High | Touches every remote invocation in an 1800-line production deploy script; converting to bash arrays is mechanical but wide-blast-radius; current key paths contain no spaces on all three hosts. | SSH invocations use `"${SSH_OPTS[@]}"` arrays; `npm run lint:bash` + a dry-run deploy to a staging target pass. |
| D-L6 | db/app health-check loops assume `.State.Health` populated. `deploy-docker.sh:1256,1455` | LOW/Med | Latent only (both services define healthchecks today); guard pattern exists at :1570 to copy when the script is next touched for C4-US-011 decomposition (CROSS-REF). | Health loops use the `{{if .State.Health}}` guard; deploy passes. |
| D-L8 | algo app-server guard is exact-hostname-string match. `deploy-docker.sh:342-346` | LOW/Med | Defense-in-depth around already-target-aware profile envs (.env.deploy.algo sets the skip flags); driving from DEPLOY_TARGET is part of C4-US-011 deploy decomposition (CROSS-REF). | Guard keyed on DEPLOY_TARGET==algo (not hostname string); covered by deploy dry-run test. |
| D-L9 | `deploy-test-backends.sh:82-98` rsync missing `target/` exclude | LOW/High | worv/test stack removed from active roster (U1); script no longer runs against any live target. | Stack deleted, or exclude added if the script is ever revived. |
| D-L17 | `scripts/setup.sh` weak default admin/admin123 printed cleartext on `--defaults` | LOW/High | Local-dev bootstrap script (not part of production deploy path); changing default breaks documented quick-start flows — needs docs + CI fixture update together. | `--defaults` generates a random password (printed once) and docs/CI fixtures updated. |
| D-L18 | `scripts/code-similarity-rs.service` no auth token + no systemd hardening | LOW/Med | Unit file is optional bare-metal deployment artifact; both production targets run the sidecar in compose on an internal network. Hardening requires a host to validate on. | Unit file gains token env requirement + systemd sandboxing directives mirroring `online-judge.service`, validated on a host actually using it. |
| D-PR-L1 | Editing test-case content deletes+recreates row → cascade-deletes submission_results history. `src/lib/problem-management.ts:141-143` | LOW/Med | Behavior is arguably intentional (results for changed test data are invalid); switching to update-in-place changes result-invalidation semantics — needs product decision. Signature-matching keeps unchanged rows stable today. | Decision documented; if history must survive, positional update-in-place lands with a migration note and tests. |
| D-PR-L4 | `countVisibleProblemSetsForUser` / `getVisibleProblemSetIds` materialize full table to count. `src/lib/problem-sets/visibility.ts:176-178` | LOW/High | Perf-only at current data scale; the SQL-predicate refactor shares shape with the visibility filters used in list paths — do as one refactor with tests. | count() pushdown / SQL predicate lands; no full-id materialization on count path. |
| D-PR-L6 | `updateProblemWithTestCases` optional `actorId` defaults to `""` breaking file-link authz for internal callers. `problem-management.ts:333,365` | LOW/Med | All current callers pass a real actorId (route handlers); making the param required is an API-signature change rippling into scripts/tests — schedule with the next problems-module touch. | `actorId: string` required; compile-time enforced; callers audited. |
| D-CQ-L1 | `buildSqlQuery` re-parses `$N` without string-literal awareness (tx path only). `src/lib/db/queries.ts:40-49` | LOW/High | No current query embeds a literal `$N` string (grep-verified by reviewer); fix needs the literal-skipping regex consolidated from named-params.ts + tests to avoid regressing param binding. | Shared literal-aware parser used by both paths; unit test with `$1` inside a string literal. |
| D-CQ-L3 | `submissions.judgeWorkerId` indexed, relation declared, but no FK. `schema.pg.ts:487` | LOW/High | Likely intentional (worker rows are deregistered/pruned while submissions persist); adding an FK would block worker cleanup or null history. Needs decision + migration. | Schema comment documents intentional absence, or FK with ON DELETE SET NULL + migration lands. |
| D-CQ-L4 | Pending recruiting invitations with `expiresAt = NULL` never pruned (PII retention). `src/lib/data-retention-maintenance.ts:47-61` | LOW/Low | Retention-policy semantics for open-ended invites need a product decision (auto-expire after N days vs. exempt); silent behavior change could cancel valid outstanding invitations. | Policy decided; NULL-expiry invitations pruned after documented window or explicitly exempted in code comment + docs. |

## Explicitly NOT re-opened (verified fixed / clean at HEAD)

- SR notes: zip-slip, roles-only gate, per-worker judge auth, quick-create visibility, XSS/SQLi,
  secrets scan — verified fixed; C4-017 and C4-078 verified fixed (CQ status checks).
- Route audits listed under "Verified clean" in the aggregate.

## Phase D items not landed this cycle

If any Phase D item from `plan/cycle-1-2026-07-06-rpf-review-remediation.md` fails to land,
it must be moved into this file with a full record before cycle close. (updated at cycle end)
