FROM ghcr.io/ponylang/ponyc:0.61.1

USER root
RUN adduser -D -s /bin/false judge && \
    mkdir -p /workspace && chown judge:judge /workspace && \
    chmod +x $(which ponyc) && \
    chmod o+rx /usr/local/bin/ponyc 2>/dev/null || true

WORKDIR /workspace
USER judge
