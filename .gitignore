# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
test-results/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
.claude/
.omx/
.omc/
.sisyphus/

# algo problems scripts
scripts/algo-problems/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# deployment secrets (now in .env, covered by .env rule above)

# database
data/*.db
data/*.db-wal
data/*.db-shm
data/*.sqlite
data/.admin-password
data/backups/
*.sqlite
._db.sqlite

# uploaded files
data/uploads/

# rust
judge-worker-rs/target/
rate-limiter-rs/target/

# dead-letter queue (failed result payloads for manual recovery)
dead-letter/
judge-worker-rs/dead-letter/
.context/
node-compile-cache/
.agent/

# browser session exports (may contain live credentials)
algo-admin-*.json

# one-off problem-solving / verification scratch scripts (not source-of-truth)
/add-stress-tests.mjs
/auto-solver.mjs
/dedup-problems.mjs
/fetch-problems.mjs
/gen_test_cases.mjs
/solutions.js
/solve-all.mjs
/solve-all2.mjs
/solve-fixes.mjs
/solve-problems.mjs
/stress-tests.mjs
/submit.mjs
/verify-problems.mjs
/verify_all_tc.py
/verify_tc.py
/scripts/fix-copyright.mjs
/scripts/validate-enhance-201-300.mjs
/scripts/validate-enhance-basic.mjs

# NFS sentinel files (created when a file is unlinked while still open on
# another NFS client; the kernel renames the open file to .nfs.<hex> until
# the last fd closes). Surfaced this session when concurrent editors held
# deploy-docker.sh open during an Edit-driven rewrite.
.nfs*
